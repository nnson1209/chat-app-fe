"use client";

import { Typography } from "@mui/material";

export default function Logo() {
    return (
        <Typography variant="h6" fontWeight={700} letterSpacing={0.6}>
            CHAT.APP
        </Typography>
    );
}